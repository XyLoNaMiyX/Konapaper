namespace Konapaper
{
    public enum Rating { Safe, Questionable, Explicit }
    public enum StartMode { Normal, Minimized, SingleCheck }
    public enum NotificationAction { Dismiss, RemindLater, Show }
}
